%h1
  Listing links
%p.lead
  - if current_user.username.present?
    Your shareable link is
    = link_to share_url(username: current_user.username), share_url(username: current_user.username)
  - else
    Please set your username by editting your profile to create a shareable link.

%table.table
  %thead
    %tr
      %th Title
      %th Url
      %th
      %th
      %th

  %tbody
    - @links.each do |link|
      %tr
        %td= link.title
        %td= link_to link.url, link.url
        %td= link_to 'Edit', edit_link_path(link)
        %td= link_to 'Destroy', link, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Link', new_link_path
